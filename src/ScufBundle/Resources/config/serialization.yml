ScufBundle\Entity\Access:
    attributes:
        id:
            groups: ['user', 'access']
        title:
            groups: ['user', 'access']
        slug:
            groups: ['user', 'access']
        users:
            groups: ['access']

ScufBundle\Entity\Event:
    attributes:
        id:
            groups: ['user', 'event']
        title:
            groups: ['user', 'event']
        allDay:
            groups: ['user', 'event']
        start:
            groups: ['user', 'event']
        end:
            groups: ['user', 'event']
        location:
            groups: ['user', 'event']
        backgroundColor:
            groups: ['user', 'event']
        borderColor:
            groups: ['user', 'event']
        user:
            groups: ['event']

ScufBundle\Entity\Action:
    attributes:
        id:
            groups: ['user', 'action']
        type:
            groups: ['user', 'action']
        created:
            groups: ['user', 'action']
        updated:
            groups: ['user', 'action']
        start:
            groups: ['user', 'action']
        end:
            groups: ['user', 'action']
        status:
            groups: ['user', 'action']
        view:
            groups: ['user', 'action']
        justification:
            groups: ['user', 'action']
        user:
            groups: ['action']
        recipient:
            groups: ['action']

ScufBundle\Entity\User:
    attributes:
        id:
            groups: ['user', 'access', 'event', 'auth-token']
        username:
            groups: ['user', 'access', 'event', 'auth-token']
        firstname:
            groups: ['user', 'access', 'event', 'auth-token']
        lastname:
            groups: ['user', 'access', 'event', 'auth-token']
        plainPassword:
            groups: ['user', 'access', 'event', 'auth-token']
        hoursTodo:
            groups: ['user', 'access', 'event', 'auth-token']
        hoursDone:
            groups: ['user', 'access', 'event', 'auth-token']
        hoursPlanified:
            groups: ['user', 'access', 'event', 'auth-token']
        hoursPlanifiedByMe:
            groups: ['user', 'access', 'event', 'auth-token']
        overtime:
            groups: ['user', 'access', 'event', 'auth-token']
        role:
            groups: ['user', 'access', 'event', 'auth-token']
        superior:
            groups: ['user', 'access', 'event', 'auth-token']
        action:
            groups: ['user']
        event:
            groups: ['user']
        access:
            groups: ['user']

ScufBundle\Entity\AuthToken:
    attributes:
        id:
            groups: ['auth-token']
        value:
            groups: ['auth-token']
        created:
            groups: ['auth-token']
        user:
            groups: ['auth-token']